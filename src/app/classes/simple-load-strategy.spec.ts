import { SimpleLoadStrategy } from './simple-load-strategy';

describe('SimpleLoadStrategy', () => {
  it('should create an instance', () => {
    expect(new SimpleLoadStrategy()).toBeTruthy();
  });
});
